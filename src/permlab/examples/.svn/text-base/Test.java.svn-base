/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package permlab.examples;

import java.util.HashSet;
import permlib.Permutation;

/**
 *
 * @author MichaelAlbert
 */
public class Test {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        HashSet<Permutation> basis = new HashSet<Permutation>();
        basis.add(new Permutation("2431"));
        basis.add(new Permutation("321"));
        basis.add(new Permutation("12345"));
        basis.add(new Permutation("123"));
        BubbleClass b = new BubbleClass(basis);
    }
}
